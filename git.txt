echo "# vehicle-rental-backend" >> README.md
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Asmual/vehicle-rental-backend.git
git push -u origin main


git add .
git commit -m "feat: add validation, auth, and error handling add Joi validation, JWT auth middleware, and global error handler"
git push